spring:
  datasource:
    #url: jdbc:mysql://127.0.0.1:3306/tdldb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    url: jdbc:mysql://127.0.0.1:3306/tdldb
    username: sku
    password: sku1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

#  liquibase:
#    change-log: classpath:db/db.changelog-master.yaml

logging:
  level:
    root: INFO