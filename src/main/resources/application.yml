spring:
  datasource:
    #url: jdbc:mysql://127.0.0.1:3306/tdldb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    url: jdbc:mysql://127.0.0.1:3306/data
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

#  liquibase:
#    change-log: classpath:db/db.changelog-master.yaml

logging:
  level:
    root: INFO


server.servlet.encoding.force-response : true